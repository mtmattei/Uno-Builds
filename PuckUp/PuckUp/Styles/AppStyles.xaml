<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:toolkit="using:Uno.Toolkit.UI">

    <!-- Filter Button Style -->
    <Style x:Key="FilterButtonStyle" TargetType="Button">
        <Setter Property="BorderBrush" Value="{ThemeResource OutlineColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="20" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">
                        <StackPanel Orientation="Horizontal" Margin="{TemplateBinding Padding}">
                            <FontIcon x:Name="Icon" 
                                FontFamily="Segoe MDL2 Assets" 
                                Glyph="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" 
                                Margin="0,0,8,0" 
                                FontSize="14"/>
                            <TextBlock 
                                Text="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" 
                                VerticalAlignment="Center"/>
                            <FontIcon 
                                FontFamily="Segoe MDL2 Assets" 
                                Glyph="&#xE70D;" 
                                Margin="8,0,0,0" 
                                FontSize="10"/>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Filter Flyout ListView Style -->
    <Style x:Key="FilterListViewStyle" TargetType="ListView">
        <Setter Property="Background" Value="{ThemeResource SurfaceColor}" />
        <Setter Property="BorderBrush" Value="{ThemeResource OutlineColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="Width" Value="200" />
        <Setter Property="CornerRadius" Value="4" />
    </Style>
    
    <!-- Filter ListViewItem Style -->
    <Style x:Key="FilterListViewItemStyle" TargetType="ListViewItem">
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    </Style>
    
    <!-- Filter Flyout Style -->
    <Style x:Key="FilterFlyoutStyle" TargetType="FlyoutPresenter">
        <Setter Property="Background" Value="{ThemeResource SurfaceColor}" />
        <Setter Property="BorderBrush" Value="{ThemeResource OutlineColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="CornerRadius" Value="8" />
    </Style>
    
    <!-- Standard ListView Item Style -->
    <Style x:Key="StandardListViewItemStyle" TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>
    
    <!-- Card Item Border Style -->
    <Style x:Key="CardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource SurfaceColor}" />
        <Setter Property="BorderBrush" Value="{ThemeResource OutlineColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="Margin" Value="0" />
    </Style>
    
    <!-- Position Badge Style -->
    <Style x:Key="PositionBadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource SurfaceVariantColor}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Margin" Value="0,0,8,0" />
    </Style>
    
    <!-- Game Time Badge Style -->
    <Style x:Key="GameTimeBadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource SurfaceVariantColor}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="8,4" />
    </Style>
    
    <!-- Position Needed Badge Style -->
    <Style x:Key="PositionNeededBadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource SurfaceVariantColor}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="12,6" />
    </Style>
    
    <!-- Primary Action Button Style -->
    <Style x:Key="PrimaryActionButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource PrimaryColor}" />
        <Setter Property="Foreground" Value="{ThemeResource OnPrimaryColor}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="0,8" />
    </Style>
    
    <!-- Secondary Action Button Style -->
    <Style x:Key="SecondaryActionButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource SurfaceColor}" />
        <Setter Property="BorderBrush" Value="{ThemeResource OutlineColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="16,8" />
    </Style>
</ResourceDictionary>