<Page x:Class="UnoVox.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:UnoVox.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:um="using:Uno.Material"
      NavigationCacheMode="Required"
      Background="{ThemeResource BackgroundBrush}">
  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition />
    </Grid.RowDefinitions>
    <utu:NavigationBar Content="UnoVox - Voxel Editor" />

    <StackPanel Grid.Row="1"
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Spacing="16">
      <TextBlock Text="UnoVox Voxel Editor"
                 Style="{StaticResource HeadlineTextBlockStyle}"
                 HorizontalAlignment="Center" />
      <TextBlock Text="3D Voxel Editor with Hand Tracking"
                 Style="{StaticResource BodyTextBlockStyle}"
                 HorizontalAlignment="Center"
                 Opacity="0.7" />
      <Button Content="Open Voxel Editor"
              Command="{Binding GoToVoxelEditor}"
              HorizontalAlignment="Center"
              Margin="0,24,0,0" />
    </StackPanel>
  </Grid>
</Page>
