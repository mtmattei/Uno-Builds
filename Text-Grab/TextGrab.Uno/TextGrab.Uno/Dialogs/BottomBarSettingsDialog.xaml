<ContentDialog
    x:Class="TextGrab.Dialogs.BottomBarSettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Bottom Bar Settings"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="SaveButton_Click">

  <Grid Width="700" MinHeight="400">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Two-pane list -->
    <Grid ColumnSpacing="12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <!-- Available buttons -->
      <StackPanel>
        <TextBlock Text="Available" Style="{StaticResource TitleSmall}" Margin="0,0,0,8" />
        <ListView x:Name="AvailableListView"
                  SelectionMode="Single"
                  MaxHeight="250">
          <ListView.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding ButtonText}" Margin="4,2" />
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </StackPanel>

      <!-- Add/Remove buttons -->
      <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="8">
        <Button Content="&#xE72A;" FontFamily="{ThemeResource SymbolThemeFontFamily}"
                Click="AddButton_Click" Width="40" HorizontalAlignment="Center" />
        <Button Content="&#xE72B;" FontFamily="{ThemeResource SymbolThemeFontFamily}"
                Click="RemoveButton_Click" Width="40" HorizontalAlignment="Center" />
      </StackPanel>

      <!-- Enabled buttons -->
      <StackPanel Grid.Column="2">
        <TextBlock Text="Enabled" Style="{StaticResource TitleSmall}" Margin="0,0,0,8" />
        <ListView x:Name="EnabledListView"
                  SelectionMode="Single"
                  MaxHeight="200">
          <ListView.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding ButtonText}" Margin="4,2" />
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
          <Button Content="Move Up" Click="MoveUpButton_Click" />
          <Button Content="Move Down" Click="MoveDownButton_Click" />
        </StackPanel>
      </StackPanel>
    </Grid>

    <!-- Toggle switches -->
    <StackPanel Grid.Row="1" Margin="0,16,0,0" Spacing="4">
      <TextBlock Text="Display Options" Style="{StaticResource TitleSmall}" Margin="0,0,0,8" />
      <ToggleSwitch x:Name="ShowCursorTextToggle" Header="Show Cursor Text" />
      <ToggleSwitch x:Name="ShowWordCountToggle" Header="Show Word Count" />
      <ToggleSwitch x:Name="ShowCharDetailsToggle" Header="Show Character Details" />
      <ToggleSwitch x:Name="ShowLangPickerToggle" Header="Show Language Picker" />
    </StackPanel>
  </Grid>
</ContentDialog>
