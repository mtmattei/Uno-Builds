<Page x:Class="TextGrab.Presentation.FullscreenGrabPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      NavigationCacheMode="Disabled"
      KeyDown="Page_KeyDown">

  <Page.KeyboardAccelerators>
    <KeyboardAccelerator Key="Escape" Invoked="EscAccelerator_Invoked" />
  </Page.KeyboardAccelerators>

  <Grid Background="Black">
    <!-- Zoom/pan wrapper: contains BackgroundImage + SelectionCanvas so they scale together.
         Toolbar is a sibling so it stays at fixed size and position. -->
    <Grid x:Name="ZoomWrapper"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          PointerWheelChanged="ZoomWrapper_PointerWheelChanged">
      <Grid.RenderTransform>
        <TransformGroup>
          <ScaleTransform x:Name="ZoomScale" ScaleX="1" ScaleY="1" />
          <TranslateTransform x:Name="ZoomTranslate" X="0" Y="0" />
        </TransformGroup>
      </Grid.RenderTransform>

      <!-- Screen capture image fills entire page -->
      <Image x:Name="BackgroundImage"
             Stretch="UniformToFill"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch" />

      <!-- Semi-transparent overlay Canvas for region selection -->
      <Canvas x:Name="SelectionCanvas"
              Background="Transparent"
              PointerPressed="Canvas_PointerPressed"
              PointerMoved="Canvas_PointerMoved"
              PointerReleased="Canvas_PointerReleased">

      <!-- Four shade rectangles that darken everything *outside* the selection.
           When SelectionBorder is collapsed, ShadeTop covers the whole canvas. -->
      <Rectangle x:Name="ShadeTop"    Fill="{StaticResource ShadeOverlayBrush}" IsHitTestVisible="False" />
      <Rectangle x:Name="ShadeBottom" Fill="{StaticResource ShadeOverlayBrush}" IsHitTestVisible="False" />
      <Rectangle x:Name="ShadeLeft"   Fill="{StaticResource ShadeOverlayBrush}" IsHitTestVisible="False" />
      <Rectangle x:Name="ShadeRight"  Fill="{StaticResource ShadeOverlayBrush}" IsHitTestVisible="False" />

      <!-- Selection rectangle -->
      <Border x:Name="SelectionBorder"
              BorderBrush="{ThemeResource PrimaryBrush}"
              BorderThickness="2"
              Background="Transparent"
              Visibility="Collapsed" />

      <!-- Size readout during drag -->
      <Border x:Name="SizeReadout"
              Background="{StaticResource ToolbarBackgroundBrush}"
              CornerRadius="4"
              Padding="6,2"
              IsHitTestVisible="False"
              Visibility="Collapsed">
        <TextBlock x:Name="SizeReadoutText"
                   Foreground="White"
                   FontSize="12"
                   FontWeight="SemiBold"
                   Text="" />
      </Border>

      <Canvas.ContextFlyout>
        <MenuFlyout>
          <ToggleMenuFlyoutItem x:Name="SingleLineCtxItem" Text="Single Line Mode" />
          <ToggleMenuFlyoutItem x:Name="TableCtxItem" Text="Table Mode" />
          <ToggleMenuFlyoutItem x:Name="FreezeCtxItem" Text="Freeze (F)" Click="Freeze_Click" />
          <ToggleMenuFlyoutItem x:Name="SendToEtwCtxItem" Text="Send to Edit Text" />
          <MenuFlyoutSeparator />
          <MenuFlyoutItem Text="Settings" Click="Settings_Click" />
          <MenuFlyoutItem Text="Cancel" Click="Cancel_Click" />
        </MenuFlyout>
      </Canvas.ContextFlyout>
      </Canvas>
    </Grid>

    <!-- Floating toolbar (top center, dark translucent) -->
    <Border x:Name="FloatingToolbar"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0,12,0,0"
            Padding="12,6"
            CornerRadius="8"
            Background="{StaticResource ToolbarBackgroundBrush}"
            ManipulationMode="TranslateX,TranslateY"
            Translation="0,0,32">
      <Border.RenderTransform>
        <TranslateTransform x:Name="ToolbarTranslate" />
      </Border.RenderTransform>
      <Border.Shadow>
        <ThemeShadow />
      </Border.Shadow>
      <StackPanel Orientation="Horizontal" Spacing="8">
        <FontIcon Glyph="&#xE76F;"
                  FontFamily="{ThemeResource SymbolThemeFontFamily}"
                  Foreground="{StaticResource ToolbarHintBrush}"
                  FontSize="14"
                  VerticalAlignment="Center"
                  Margin="0,0,4,0"
                  IsHitTestVisible="False"
                  ToolTipService.ToolTip="Drag to move toolbar" />
        <ComboBox x:Name="LanguagesComboBox"
                  MinWidth="180"
                  PlaceholderText="Language"
                  DisplayMemberPath="DisplayName"
                  SelectionChanged="LanguagesComboBox_SelectionChanged" />

        <Border Width="1" Background="{StaticResource ToolbarSeparatorBrush}" Margin="4,2" />

        <RadioButton x:Name="NormalModeRadio" Content="Standard" IsChecked="True"
                     GroupName="GrabMode" Foreground="White" MinWidth="0" Padding="4,0" />
        <RadioButton x:Name="SingleLineModeRadio" Content="Single Line"
                     GroupName="GrabMode" Foreground="White" MinWidth="0" Padding="4,0" />
        <RadioButton x:Name="TableModeRadio" Content="Table"
                     GroupName="GrabMode" Foreground="White" MinWidth="0" Padding="4,0" />

        <Border Width="1" Background="{StaticResource ToolbarSeparatorBrush}" Margin="4,2" />

        <ToggleButton x:Name="FreezeToggle" Content="&#xE786;"
                      FontFamily="{ThemeResource SymbolThemeFontFamily}"
                      ToolTipService.ToolTip="Freeze (F) — re-capture without overlay"
                      Click="Freeze_Click" />

        <ToggleButton x:Name="GrabFrameToggle" Content="&#xE8B9;"
                      FontFamily="{ThemeResource SymbolThemeFontFamily}"
                      ToolTipService.ToolTip="Grab Frame (G) — draw a region to open it as a Grab Frame"
                      Click="GrabFrame_Click" />

        <ToggleButton x:Name="SendToEtwToggle" Content="&#xE8A7;"
                      FontFamily="{ThemeResource SymbolThemeFontFamily}"
                      ToolTipService.ToolTip="Send to Edit Text (E)" />

        <DropDownButton x:Name="PostGrabActionsButton"
                        Content="&#xE90F;"
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        ToolTipService.ToolTip="Post-grab actions">
          <DropDownButton.Flyout>
            <MenuFlyout x:Name="PostGrabActionsFlyout" />
          </DropDownButton.Flyout>
        </DropDownButton>

        <Button Content="&#xE713;" FontFamily="{ThemeResource SymbolThemeFontFamily}"
                ToolTipService.ToolTip="Settings"
                Click="Settings_Click" />

        <Button Content="&#xE711;" FontFamily="{ThemeResource SymbolThemeFontFamily}"
                ToolTipService.ToolTip="Cancel (Esc)"
                Click="Cancel_Click" />
      </StackPanel>
    </Border>

    <!-- Status bar (bottom center, dark translucent) -->
    <Border HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Margin="0,0,0,12"
            Padding="16,8"
            CornerRadius="8"
            Background="{StaticResource ToolbarBackgroundBrush}">
      <StackPanel Orientation="Horizontal" Spacing="12">
        <TextBlock x:Name="StatusText"
                   Text="Draw a rectangle to capture text, or press Esc to cancel"
                   Foreground="White"
                   Style="{StaticResource BodyMedium}" />
        <ProgressRing x:Name="BusyRing" IsActive="False"
                      Width="16" Height="16" />
      </StackPanel>
    </Border>

    <!-- Non-Windows fallback -->
    <StackPanel x:Name="FallbackPanel"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="12"
                Visibility="Collapsed">
      <TextBlock Text="Screen capture requires Windows Desktop."
                 Style="{StaticResource TitleMedium}"
                 Foreground="White"
                 HorizontalAlignment="Center" />
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
        <Button Content="OCR from File" Click="OcrFromFile_Click" />
        <Button Content="OCR from Clipboard" Click="OcrFromClipboard_Click" />
        <Button Content="Back" Click="Cancel_Click" />
      </StackPanel>
    </StackPanel>
  </Grid>
</Page>
