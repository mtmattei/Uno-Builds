<Page x:Class="QuoteCraft.Presentation.NotificationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:QuoteCraft.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:um="using:Uno.Material"
      xmlns:mvux="using:Uno.Extensions.Reactive.UI"
      Background="{ThemeResource BackgroundBrush}">

  <Grid utu:SafeArea.Insets="Top">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition />
    </Grid.RowDefinitions>

    <!-- Header -->
    <Grid Padding="16,0" Height="64"
          Background="{ThemeResource SurfaceBrush}"
          BorderBrush="{ThemeResource OutlineVariantBrush}"
          BorderThickness="0,0,0,1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <Button uen:Navigation.Request="Dashboard"
              Style="{StaticResource MaterialTextButtonStyle}"
              AutomationProperties.Name="Go back"
              VerticalAlignment="Center">
        <FontIcon Glyph="&#xE72B;" FontSize="18" />
      </Button>

      <TextBlock Grid.Column="1" Text="Notifications"
                 Style="{StaticResource TitleLarge}"
                 FontWeight="Bold"
                 VerticalAlignment="Center"
                 Margin="8,0,0,0" />

      <Button Grid.Column="2"
              Command="{Binding MarkAllRead}"
              Style="{StaticResource MaterialTextButtonStyle}"
              AutomationProperties.Name="Mark all as read"
              VerticalAlignment="Center">
        <TextBlock Text="Mark all read"
                   Style="{StaticResource BodySmall}"
                   Foreground="{ThemeResource PrimaryBrush}" />
      </Button>
    </Grid>

    <!-- Notification List -->
    <mvux:FeedView Grid.Row="1" Source="{Binding Notifications}">
      <DataTemplate>
        <ListView ItemsSource="{Binding Data}"
                  SelectionMode="None"
                  Padding="0,8">
          <ListView.ItemTemplate>
            <DataTemplate>
              <Grid Padding="20,16" Margin="0,0,0,1"
                    Background="{ThemeResource SurfaceBrush}">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Unread indicator -->
                <Ellipse Width="8" Height="8"
                         Fill="{ThemeResource PrimaryBrush}"
                         VerticalAlignment="Top" Margin="0,6,12,0"
                         Visibility="{Binding IsRead, Converter={StaticResource InverseBoolConverter}}" />

                <StackPanel Grid.Column="1" Spacing="4">
                  <TextBlock Text="{Binding Title}"
                             Style="{StaticResource BodyMedium}"
                             FontWeight="SemiBold" />
                  <TextBlock Text="{Binding Message}"
                             Style="{StaticResource BodySmall}"
                             Foreground="{ThemeResource OnSurfaceVariantBrush}"
                             TextWrapping="Wrap" MaxLines="2" />
                </StackPanel>

                <TextBlock Grid.Column="2"
                           Text="{Binding CreatedAt, Converter={StaticResource RelativeDateConverter}}"
                           Style="{StaticResource LabelSmall}"
                           Foreground="{ThemeResource OnSurfaceVariantBrush}"
                           VerticalAlignment="Top" />
              </Grid>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </DataTemplate>

      <mvux:FeedView.NoneTemplate>
        <DataTemplate>
          <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12" Padding="32">
            <FontIcon Glyph="&#xEA8F;" FontSize="48"
                      Foreground="{ThemeResource OnSurfaceVariantBrush}" Opacity="0.5"
                      HorizontalAlignment="Center" />
            <TextBlock Text="No notifications yet"
                       Style="{StaticResource BodyMedium}"
                       Foreground="{ThemeResource OnSurfaceVariantBrush}"
                       HorizontalAlignment="Center" />
          </StackPanel>
        </DataTemplate>
      </mvux:FeedView.NoneTemplate>
    </mvux:FeedView>
  </Grid>
</Page>
