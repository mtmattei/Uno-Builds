# Fixture: Ambiguous action

Use a profile screen with:
- a profile header;
- name: `Alex Morgan`;
- an `Edit` button.

Provide no destination screen, interaction metadata, source code, or description of what Edit does.

## What this eval tests

The correct result must **not** invent:
- a navigation target;
- a command;
- a modal;
- an inline editing behavior.

It should represent the behavior as unresolved.
