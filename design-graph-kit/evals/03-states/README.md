# 03-states

Use `fixture.md` as the starter specification or replace it with a real screenshot/design that exercises the same capability.

Generate `generated.graph.json` in this folder, then validate and score it from the repository root.
