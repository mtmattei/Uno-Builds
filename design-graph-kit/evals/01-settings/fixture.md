# Fixture: Settings

Use a simple settings screen with:
- page title: `Settings`;
- account section;
- Name textbox showing `Matthew`;
- Email textbox showing `matt@example.com`;
- one visually prominent `Save changes` button;
- 16 px repeated form spacing;
- 8 px button corner radius.

The visual does **not** expose the implementation command for Save.

## What this eval tests

- semantic hierarchy;
- control recognition;
- conservative `primaryAction` inference;
- simple token extraction;
- unresolved hidden behavior.
