<UserControl x:Class="FieldOpsPro.Presentation.Controls.StatusHeroCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:FieldOpsPro.Presentation.Controls"
             xmlns:utu="using:Uno.Toolkit.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

  <Border CornerRadius="4"
          Padding="24"
          MinHeight="140"
          Background="#FFFFFF">

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <!-- Status Label -->
      <TextBlock x:Name="StatusLabel"
                 Grid.Row="0"
                 Text="CURRENT STATUS"
                 FontSize="10"
                 FontWeight="Normal"
                 CharacterSpacing="80"
                 Opacity="0.5"
                 Foreground="#000000" />

      <!-- Status Title -->
      <TextBlock x:Name="StatusTitle"
                 Grid.Row="1"
                 Text="En Route to Site"
                 FontSize="20"
                 FontWeight="Light"
                 Foreground="#000000"
                 Margin="0,8,0,0" />

      <!-- Destination Section -->
      <StackPanel Grid.Row="2" Margin="0,20,0,0">
        <TextBlock Text="NEXT DESTINATION"
                   FontSize="10"
                   FontWeight="Normal"
                   CharacterSpacing="80"
                   Opacity="0.4"
                   Foreground="#000000" />

        <TextBlock x:Name="DestinationText"
                   FontSize="14"
                   FontWeight="Normal"
                   Foreground="#000000"
                   Margin="0,6,0,0" />

        <TextBlock x:Name="EtaText"
                   FontSize="12"
                   Opacity="0.6"
                   Foreground="#000000"
                   Margin="0,2,0,0" />
      </StackPanel>

      <!-- Action Button - minimal outline style -->
      <Border Grid.Row="3"
              Width="44"
              Height="44"
              CornerRadius="4"
              HorizontalAlignment="Right"
              VerticalAlignment="Bottom"
              BorderBrush="#000000"
              BorderThickness="1"
              Background="Transparent">
        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                  Glyph="&#xE816;"
                  FontSize="18"
                  Foreground="#000000"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center" />
      </Border>
    </Grid>
  </Border>
</UserControl>
