<UserControl x:Class="FluxTransit.Presentation.Shell"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:FluxTransit.Presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      mc:Ignorable="d"
      d:DesignHeight="800"
      d:DesignWidth="1200">
  <Border Background="{StaticResource FluxBackgroundBrush}">
    <utu:ExtendedSplashScreen x:Name="Splash"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch">
      <utu:ExtendedSplashScreen.LoadingContentTemplate>
        <DataTemplate>
          <Grid Background="{StaticResource FluxBackgroundBrush}">
            <Grid.RowDefinitions>
              <RowDefinition Height="2*" />
              <RowDefinition />
            </Grid.RowDefinitions>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16">
              <TextBlock Text="FLUX TRANSIT"
                         Style="{StaticResource FluxHeadingLarge}"
                         HorizontalAlignment="Center" />
              <TextBlock Text="Montreal Transit Dashboard"
                         Style="{StaticResource FluxBody}"
                         HorizontalAlignment="Center" />
            </StackPanel>

            <ProgressRing IsActive="True"
                  Grid.Row="1"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Center"
                  Height="60"
                  Width="60"
                  Foreground="{StaticResource FluxPrimaryBrush}" />
          </Grid>
        </DataTemplate>
      </utu:ExtendedSplashScreen.LoadingContentTemplate>

      <!-- Main Shell Content with TabBar Navigation -->
      <Grid uen:Region.Attached="True">
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Content Region -->
        <Border Grid.Row="0"
                uen:Region.Attached="True"
                uen:Region.Navigator="Visibility" />

        <!-- Bottom TabBar -->
        <utu:TabBar Grid.Row="1"
                    uen:Region.Attached="True"
                    Style="{StaticResource BottomTabBarStyle}"
                    Background="{StaticResource FluxSurfaceBrush}"
                    SelectedIndex="0">
          <utu:TabBar.Items>
            <utu:TabBarItem uen:Region.Name="Dashboard"
                            Content="Dashboard">
              <utu:TabBarItem.Icon>
                <FontIcon Glyph="&#xE80F;" />
              </utu:TabBarItem.Icon>
            </utu:TabBarItem>
            <utu:TabBarItem uen:Region.Name="Profile"
                            Content="Profile">
              <utu:TabBarItem.Icon>
                <FontIcon Glyph="&#xE77B;" />
              </utu:TabBarItem.Icon>
            </utu:TabBarItem>
          </utu:TabBar.Items>
        </utu:TabBar>
      </Grid>
    </utu:ExtendedSplashScreen>
  </Border>
</UserControl>
