<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- CRT Color Resources -->
  <Color x:Key="CrtBackground">#050A06</Color>
  <Color x:Key="CrtBackgroundDeep">#030703</Color>
  <Color x:Key="PhosphorPrimary">#33FF66</Color>
  <Color x:Key="PhosphorBright">#66FFAA</Color>
  <Color x:Key="PhosphorDim">#1A9940</Color>
  <Color x:Key="PhosphorGlow">#5933FF66</Color>
  <Color x:Key="PhosphorSubtle">#1433FF66</Color>
  <Color x:Key="BezelPrimary">#1A1A1A</Color>
  <Color x:Key="BezelEdge">#2A2A2A</Color>
  <Color x:Key="BezelLabel">#555555</Color>
  <Color x:Key="ScanLineOverlayColor">#26000000</Color>
  <Color x:Key="BorderSubtle">#3333FF66</Color>
  <Color x:Key="BorderActive">#33FF66</Color>

  <SolidColorBrush x:Key="CrtBackgroundBrush" Color="{StaticResource CrtBackground}" />
  <SolidColorBrush x:Key="CrtBackgroundDeepBrush" Color="{StaticResource CrtBackgroundDeep}" />
  <SolidColorBrush x:Key="PhosphorPrimaryBrush" Color="{StaticResource PhosphorPrimary}" />
  <SolidColorBrush x:Key="PhosphorBrightBrush" Color="{StaticResource PhosphorBright}" />
  <SolidColorBrush x:Key="PhosphorDimBrush" Color="{StaticResource PhosphorDim}" />
  <SolidColorBrush x:Key="PhosphorGlowBrush" Color="{StaticResource PhosphorGlow}" />
  <SolidColorBrush x:Key="PhosphorSubtleBrush" Color="{StaticResource PhosphorSubtle}" />
  <SolidColorBrush x:Key="BezelPrimaryBrush" Color="{StaticResource BezelPrimary}" />
  <SolidColorBrush x:Key="BezelEdgeBrush" Color="{StaticResource BezelEdge}" />
  <SolidColorBrush x:Key="BezelLabelBrush" Color="{StaticResource BezelLabel}" />
  <SolidColorBrush x:Key="BorderSubtleBrush" Color="{StaticResource BorderSubtle}" />
  <SolidColorBrush x:Key="BorderActiveBrush" Color="{StaticResource BorderActive}" />

  <!-- Font Families -->
  <FontFamily x:Key="OrbitronFont">ms-appx:///Assets/Fonts/Orbitron-Variable.ttf#Orbitron</FontFamily>
  <FontFamily x:Key="ShareTechMonoFont">ms-appx:///Assets/Fonts/ShareTechMono-Regular.ttf#Share Tech Mono</FontFamily>

  <!-- CRT Text Styles -->
  <Style x:Key="CrtDisplayXL" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource OrbitronFont}" />
    <Setter Property="FontSize" Value="72" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="Foreground" Value="{StaticResource PhosphorBrightBrush}" />
  </Style>

  <Style x:Key="CrtDisplayLG" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource OrbitronFont}" />
    <Setter Property="FontSize" Value="48" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="Foreground" Value="{StaticResource PhosphorBrightBrush}" />
  </Style>

  <Style x:Key="CrtDisplayMD" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource OrbitronFont}" />
    <Setter Property="FontSize" Value="28" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{StaticResource PhosphorPrimaryBrush}" />
  </Style>

  <Style x:Key="CrtHeading" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource OrbitronFont}" />
    <Setter Property="FontSize" Value="15" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="CharacterSpacing" Value="150" />
    <Setter Property="Foreground" Value="{StaticResource PhosphorBrightBrush}" />
  </Style>

  <Style x:Key="CrtBody" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource ShareTechMonoFont}" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="Foreground" Value="{StaticResource PhosphorPrimaryBrush}" />
  </Style>

  <Style x:Key="CrtLabel" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource ShareTechMonoFont}" />
    <Setter Property="FontSize" Value="10" />
    <Setter Property="CharacterSpacing" Value="100" />
    <Setter Property="Foreground" Value="{StaticResource PhosphorDimBrush}" />
  </Style>

  <Style x:Key="CrtLogEntry" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource ShareTechMonoFont}" />
    <Setter Property="FontSize" Value="9" />
    <Setter Property="Foreground" Value="{StaticResource PhosphorDimBrush}" />
  </Style>

  <!-- CRT Button Style with hover/press glow -->
  <Style x:Key="CrtButtonStyle" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{StaticResource PhosphorPrimaryBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource PhosphorDimBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="2" />
    <Setter Property="MinHeight" Value="44" />
    <Setter Property="MinWidth" Value="44" />
    <Setter Property="FontFamily" Value="{StaticResource ShareTechMonoFont}" />
    <Setter Property="Padding" Value="8,4" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="RootBorder"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{TemplateBinding CornerRadius}"
                  Padding="{TemplateBinding Padding}">
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="PointerOver">
                  <Storyboard>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Background">
                      <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PhosphorSubtleBrush}" />
                    </ObjectAnimationUsingKeyFrames>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="BorderBrush">
                      <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PhosphorPrimaryBrush}" />
                    </ObjectAnimationUsingKeyFrames>
                  </Storyboard>
                </VisualState>
                <VisualState x:Name="Pressed">
                  <Storyboard>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Background">
                      <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PhosphorGlowBrush}" />
                    </ObjectAnimationUsingKeyFrames>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="BorderBrush">
                      <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PhosphorBrightBrush}" />
                    </ObjectAnimationUsingKeyFrames>
                  </Storyboard>
                </VisualState>
                <VisualState x:Name="Disabled">
                  <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Opacity"
                                     To="0.4" Duration="0:0:0.15" />
                  </Storyboard>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <ContentPresenter Content="{TemplateBinding Content}"
                              ContentTemplate="{TemplateBinding ContentTemplate}"
                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- CRT Touch Zone Button Style -->
  <Style x:Key="CrtTouchZoneStyle" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{StaticResource PhosphorDimBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="2" />
    <Setter Property="MinHeight" Value="48" />
    <Setter Property="MinWidth" Value="80" />
    <Setter Property="FontFamily" Value="{StaticResource ShareTechMonoFont}" />
    <Setter Property="FontSize" Value="10" />
    <Setter Property="Padding" Value="12,6" />
  </Style>

</ResourceDictionary>
