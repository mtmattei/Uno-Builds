<Page x:Class="ListHold.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ListHold"
      xmlns:controls="using:ListHold.Controls"
      xmlns:viewmodels="using:ListHold.ViewModels"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource BackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid utu:SafeArea.Insets="VisibleBounds"
              Padding="24,48"
              HorizontalAlignment="Center"
              MaxWidth="648">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,32">
                <TextBlock Text="HOT REVEAL"
                           Style="{StaticResource PageTitleStyle}" />
                <TextBlock Text="Press and hold to reveal more details"
                           Style="{StaticResource PageDescriptionStyle}"
                           Margin="0,4,0,0" />
            </StackPanel>

            <!-- List -->
            <ItemsRepeater x:Name="ItemsRepeater"
                           Grid.Row="1">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:ListItemViewModel">
                        <controls:RevealableListItem ViewModel="{x:Bind}" />
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </Grid>
    </ScrollViewer>
</Page>