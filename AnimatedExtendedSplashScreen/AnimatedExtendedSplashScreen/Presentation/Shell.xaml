<UserControl
    x:Class="AnimatedExtendedSplashScreen.Presentation.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctwul="using:CommunityToolkit.WinUI.Lottie"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:AnimatedExtendedSplashScreen.Presentation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utu="using:Uno.Toolkit.UI"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Border Background="{ThemeResource BackgroundBrush}">
        <utu:ExtendedSplashScreen
            x:Name="Splash"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch">
            <utu:ExtendedSplashScreen.LoadingContentTemplate>
                <DataTemplate>
                    <Grid Background="Black">
                    <AnimatedVisualPlayer>
                        <ctwul:LottieVisualSource UriSource="ms-appx:///Assets/netflix.json" />
                    </AnimatedVisualPlayer>
                    </Grid>
                </DataTemplate>
            </utu:ExtendedSplashScreen.LoadingContentTemplate>
        </utu:ExtendedSplashScreen>
    </Border>
</UserControl>
