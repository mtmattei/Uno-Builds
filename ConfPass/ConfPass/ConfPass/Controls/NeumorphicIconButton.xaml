<UserControl x:Class="ConfPass.Controls.NeumorphicIconButton"
             x:Name="RootControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:utu="using:Uno.Toolkit.UI">

    <Grid x:Name="RootGrid"
          PointerPressed="OnPointerPressed"
          PointerReleased="OnPointerReleased"
          PointerCanceled="OnPointerCanceled"
          PointerCaptureLost="OnPointerCaptureLost">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="Pressed">
                    <VisualState.Setters>
                        <Setter Target="RaisedContainer.Opacity" Value="0" />
                        <Setter Target="InsetContainer.Opacity" Value="1" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <utu:ShadowContainer x:Name="RaisedContainer"
                             Background="{ThemeResource NeumorphicBgBrush}"
                             Shadows="{ThemeResource NeuroRaisedMd}"
                             Padding="6">
            <Grid x:Name="RaisedContent"
                  Width="44" Height="44"
                  CornerRadius="{StaticResource RadiusSm}">
                <ContentPresenter x:Name="RaisedContentPresenter"
                                  Content="{Binding IconContent, ElementName=RootControl}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
            </Grid>
        </utu:ShadowContainer>

        <utu:ShadowContainer x:Name="InsetContainer"
                             Background="{ThemeResource NeumorphicBgBrush}"
                             Shadows="{ThemeResource NeuroInsetMd}"
                             Opacity="0">
            <Grid x:Name="InsetContent"
                  Width="44" Height="44"
                  Margin="6"
                  CornerRadius="{StaticResource RadiusSm}">
                <ContentPresenter x:Name="InsetContentPresenter"
                                  Content="{Binding IconContent, ElementName=RootControl}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
            </Grid>
        </utu:ShadowContainer>
    </Grid>
</UserControl>
