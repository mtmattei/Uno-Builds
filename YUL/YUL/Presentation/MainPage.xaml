<Page x:Class="YUL.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:YUL.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:um="using:Uno.Material"
      NavigationCacheMode="Required"
      Background="{ThemeResource BackgroundBrush}">
  
  <Page.Resources>
    <Storyboard x:Name="SlideInStoryboard">
      <DoubleAnimation Storyboard.TargetName="NotificationTransform"
                       Storyboard.TargetProperty="Y"
                       From="-100"
                       To="0"
                       Duration="0:0:0.4"
                       EnableDependentAnimation="True">
        <DoubleAnimation.EasingFunction>
          <CubicEase EasingMode="EaseOut" />
        </DoubleAnimation.EasingFunction>
      </DoubleAnimation>
      <DoubleAnimation Storyboard.TargetName="BoardingPassNotification"
                       Storyboard.TargetProperty="Opacity"
                       From="0"
                       To="1"
                       Duration="0:0:0.3" />
    </Storyboard>
  </Page.Resources>
  
  <Grid>
    <Grid utu:SafeArea.Insets="VisibleBounds">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition />
      </Grid.RowDefinitions>
      <utu:NavigationBar Content="{Binding Title}" />

      <StackPanel Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16">
        <Button Content="Send Notification"
            AutomationProperties.AutomationId="SendNotificationButton"
            Click="OnSendNotificationClick" />
      </StackPanel>
    </Grid>
    
    <!-- Boarding Pass Notification Overlay -->
    <local:BoardingPassPage x:Name="BoardingPassNotification"
                            Visibility="Collapsed"
                            Opacity="0">
      <local:BoardingPassPage.RenderTransform>
        <TranslateTransform x:Name="NotificationTransform" Y="-100" />
      </local:BoardingPassPage.RenderTransform>
    </local:BoardingPassPage>
  </Grid>
</Page>
