<Page xmlns:markup="using:Uno.UI.Markup" x:Class="SalesDashboard.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SalesDashboard.Presentation"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{StaticResource SurfaceVariantColor}">

    <Grid utu:SafeArea.Insets="All" Padding="24">
    <local:SalesDashboardWidget HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="400" />
    <Grid.DataContext><markup:FromJson><markup:FromJson.Source>{}</markup:FromJson.Source></markup:FromJson></Grid.DataContext>
</Grid>
</Page>